# Required — your Azure AD tenant ID
AZURE_TENANT_ID=<your-tenant-id>

# Optional — for app registration auth instead of az login
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=

# Optional — for real token exchange in Token Simulator
# Provide the blueprint's credential for T1 token acquisition
BLUEPRINT_CLIENT_SECRET=
BLUEPRINT_CERTIFICATE_PATH=
